# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
.env.local
!.env.example
# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local-only architecture notes (see A9 / L14)
LOCAL_NOTES.md

# local-only audit reports — contain quoted secrets, never commit
phase1-report.md
phase2-clickpath.md
phase2-coverage.md
phase2-simplify.md

# Claude Code runtime state
.claude/

# local-only diploma report drafts (Kazakh/Russian) + scratch parts — never commit
*дипломдық*.md
*преддипломной*.md
*дипломный*.md
*отчет*.md
_part*.md

